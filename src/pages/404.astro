---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout>
  <div class="flex flex-col items-center justify-center text-center py-20">
    <h1 class="text-6xl md:text-8xl font-bold text-sky-600 dark:text-sky-400">
      404
    </h1>

    <div id="content-ja" class="hidden">
      <h2
        class="mt-4 text-3xl md:text-4xl font-bold text-gray-800 dark:text-gray-100"
      >
        ページが見つかりません
      </h2>
      <p class="mt-4 max-w-md text-gray-600 dark:text-gray-300">
        お探しのページは、移動または削除された可能性があります。
        URLをご確認の上、再度お試しください。
      </p>
      <a
        href="/ja/"
        class="mt-8 bg-sky-600 hover:bg-sky-700 text-white font-bold py-3 px-8 rounded-lg transition-colors inline-flex items-center"
      >
        ホームページに戻る
      </a>
    </div>

    <div id="content-en" class="hidden">
      <h2
        class="mt-4 text-3xl md:text-4xl font-bold text-gray-800 dark:text-gray-100"
      >
        Page Not Found
      </h2>
      <p class="mt-4 max-w-md text-gray-600 dark:text-gray-300">
        The page you are looking for might have been moved or deleted. Please
        check the URL and try again.
      </p>
      <a
        href="/en/"
        class="mt-8 bg-sky-600 hover:bg-sky-700 text-white font-bold py-3 px-8 rounded-lg transition-colors inline-flex items-center"
      >
        Back to Homepage
      </a>
    </div>
  </div>
</BaseLayout>

<script>
  const path = window.location.pathname;
  let langToShow = "ja";

  if (path.startsWith("/en")) {
    langToShow = "en";
  }

  const contentElement = document.getElementById(`content-${langToShow}`);
  if (contentElement) {
    contentElement.classList.remove("hidden");
  } else {
    const fallbackElement = document.getElementById("content-ja");
    if (fallbackElement) {
      fallbackElement.classList.remove("hidden");
    }
  }
</script>
